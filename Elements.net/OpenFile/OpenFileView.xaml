<UserControl x:Class="Elements.net.OpenFile.OpenFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:commands="clr-namespace:Elements.net.Commands"
             xmlns:local="clr-namespace:Elements.net.OpenFile"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300"
             >
    <UserControl.Resources>
        <local:OpenFileVm x:Key="ViewModel"/>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0 0 4 4"/>
        </Style>
    </UserControl.Resources>

    <Grid DataContext="{StaticResource ViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button CommandParameter="Wohooo"
                    Command="{Binding Path=OpenSource}"
                    Content="Open Source"
                    />
            <Button Grid.Row="1"
                    CommandParameter="true"
                    Command="{Binding Path=ExitApp}"
                    Content="Exit"
                    />
        </Grid>
        <Grid TextBoxBase.TextChanged="TextBoxBase_OnTextChanged"  Column="1">
            <!--<Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>-->
            <TextBox Text="{Binding Path=SourceText}"
                     IsReadOnly="True"
                     />
        </Grid>

    </Grid>
</UserControl>
