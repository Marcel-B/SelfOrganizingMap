<UserControl x:Class="Elements.net.MenuView.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Elements.net.MenuView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:MenuViewModel x:Key="ViewModel"/>
        <!--<Style TargetType="Button">
            <Setter Property="Margin" Value="0 0 4 4"/>
        </Style>-->
    </UserControl.Resources>

        <Menu DataContext="{StaticResource ViewModel}">
        <MenuItem Header="_Datei">
            <MenuItem Header="_Laden"
                      CommandParameter="true"
                      Command="{Binding Path=OpenSource}"/>
            <MenuItem Header="_Speichern"/>
            <Separator/>
            <MenuItem Header="_Beenden"
                      CommandParameter="true"
                      Command="{Binding Path=ExitApp}"/>
        </MenuItem>
    </Menu>
</UserControl>
